# FixProof QA evidence — updated 16 September 2026

## Result

Working local MVP at http://127.0.0.1:8768. Real Qwen 3.5 4B inference through the existing local Ollama service; no paid API or model downloads. All three exact-model source manuals are documented in [FIXPROOF.md](FIXPROOF.md). This is implementation evidence, not customer validation or a physical appliance test.

## Actual Chrome UI checks

| Journey | Observed result |
| --- | --- |
| New fictional demo case | Saved with explicit fictional model identity and Open status |
| Ask for drying assessment | Real AI trace and manual-linked check visible; zero outcomes until explicit user action |
| Record waiting outcome | Fictional observation and Still wet saved; issue stayed Open |
| Reload browser / restart app process | Same case, history, outcome and revision restored |
| Natural follow-up after waiting | Final implementation selected rinse aid, excluding the recorded waiting check |
| Record Not yet tested | Export/record used that label, not completed or resolved |
| Prepare handover / preview | Handover ready; unresolved statement, model provenance, user notes and outcomes visible |
| Reopen and update deferred outcome | Rinse aid changed from Not yet tested to Still wet; earlier event retained in history |
| Unknown, unconfirmed model | No applicable reference warning; request received scope response and no check |
| Model endpoint deliberately unavailable | Visible error, revision unchanged, no assistant reply fabricated, message retained |
| Reload after failed request, restore provider, retry | Draft survived; retry created one reply at revision 1 and cleared draft |
| Two tabs editing same case | Old revision could not overwrite Handover ready with resolved; visible reload instruction |
| Desktop screenshot inspection | Readable two-column layout; no overlapping controls observed at tested desktop size |
| Voice-first UI | Chrome exposed an enabled Speak control, review-before-send privacy text, and typed fallback |
| Spoken response | Browser action completed and the live status returned to Ready; audible output could not be independently measured |
| Hosted judge-mode shortcuts | Plastic, safety hazard, unsupported issue, and unclear report each reached the intended bounded response |
| Safety stop persistence | Hazard language produced an explicit stop/handover response; after reload the case remained Handover ready and the ask form stayed unavailable |
| Handover completeness | Preview showed the exact model, recorded outcome, source, observation, and unresolved limits without overlap |
| Portable browser evidence | Production Site version 31 exports a versioned JSON bundle, recomputes its full SHA-256 in a recipient-side browser view, renders bounded case facts when the digest matches, and reports a mismatch after one evidence field changes; the no-authorship boundary remains visible |
| Fifth wash-noise path | Production Chrome selected SMS6HCI02A/72, returned manual page 49, saved an explicit unchanged outcome, and restored the same model, outcome and citation after reload |
| Sixth cutlery-rust path | Production Chrome selected SMS6HCI02A/72, returned the manufacturer-backed rust-resistant-tableware check on manual page 46, saved an explicit unchanged outcome, and restored the same model, outcome and citation after reload |
| Seventh irreversible-glass-clouding path | Production Chrome selected SMS6HCI02A/72, returned `Use dishwasher-proof glasses` on manual page 46, saved an explicit unchanged outcome, rendered the observation and source in the handover, and restored them after reload |
| Eighth unpleasant-interior-odour path | Production Chrome selected SMS6HCI01A/38, prefilled the odour report, exposed all three routine-care checks, returned `Remove coarse interior soiling` on manual page 38, saved an explicit unchanged outcome and preserved it in the fingerprinted handover |
| Ninth door-related-starting path | Production Chrome selected SMS6HCI02A/72, prefilled the door-related starting report, exposed all three checks, returned `Close the appliance door` on manual page 48, saved an explicit unchanged outcome and preserved the fictional observation and citation in the handover |

Main case: `5af1e227-332c-4d5f-ab7d-6da4312082d5`. Isolated failure/recovery case: `26f996b7-e461-462e-8365-c5a6d5374ba8`; unknown-model case: `84ae5f0b-433a-4991-87d4-9b35e52e5117`, both in ignored temporary data. All notes are fictional QA observations.

The Markdown download link and the structured JSON download control were clicked in production Chrome. Their actual native download locations were not verified: Chrome's internal downloads page was blocked by browser policy, and no workaround was attempted. The Markdown export endpoint was independently checked for 200 status, content type, attachment filename, page links and factual content; its returned bytes are saved as [EXAMPLE_HANDOVER.md](EXAMPLE_HANDOVER.md). Site version 31's receiving flow accepted a valid fictional bundle through the visible paste control, matched both 64-digit digests, rendered its case, issue, model, evidence counts and reference, then reported a different recomputed digest after the issue field changed. The browser console reported no warnings or errors. The deployed `app.js` bytes matched the reviewed local release exactly by SHA-256. These checks verify the export and recipient-side verification implementation, not the browser's final disk write.

## Automated and live-model checks

- Thirty-one Python workflow and MCP regression tests pass, including separate drying, food-remnant, detergent-residue, removable-streak, wash-noise, cutlery-rust, irreversible-glass-clouding, unpleasant-interior-odour and door-related-starting selection, cross-path pending-check protection, retries, origin checks, explicit evidence recording, SQLite persistence, stale writes, model failure, invalid model output, and cross-client continuity.
- Seventeen public-interface logic tests pass against the actual hosted JavaScript, including all nine source-backed journeys, cross-path protection and the live MCP proof link.
- An official MCP client called the public `https://fixproof-mcp.keyvan-andayesh.chatgpt.site/api/mcp` deployment without local services, negotiated protocol `2026-07-28`, received server version 0.10.0 and instructions naming all nine verified paths, discovered all five tools, found the `prepare_handover` binding to `ui://fixproof/handover.html`, and read the self-contained `text/html;profile=mcp-app` resource. The same run selected third-model pages 45 and 24, retained an explicit observation across a fresh connection, excluded the recorded check, and preserved a later safety stop. The Python client independently recomputed the TypeScript handover fingerprint, matched all 64 hexadecimal digits, then changed one evidence field and obtained a different digest. Additional public cases selected wash noise on page 49, cutlery rust on page 46, irreversible glass clouding on page 46, second-model unpleasant interior odour on page 38 and third-model door-related starting on page 48. The production landing page visibly reports the inline MCP App, three exact models, nine source-backed paths and a fictional-only data policy without browser warnings or errors. See [HOSTED_MCP.json](HOSTED_MCP.json).
- The production MCP landing page's zero-setup browser proof called the live Streamable HTTP route through nine MCP requests. Chrome visibly reported successful 2025-11-25 negotiation with server 0.10.0, five discovered tools, a loaded `text/html;profile=mcp-app` resource, `Lower the rinse aid setting` with exact pages 45 and 24, one explicitly deferred outcome restored by `read_case`, and a 64-digit handover fingerprint. The probe sends only its fixed fictional model, issue and observation. Chrome reported no warnings or errors.
- The production landing page now renders that same live `ui://fixproof/handover.html` resource in a sandboxed browser preview after the nine-request proof. In Chrome, the preview visibly showed the third model, fictional streak issue, zero performed checks, one deferred check, two manual links (pages 45 and 24), the same 64-digit fingerprint as the protocol result, and the explicit no-authorship boundary. The layout was inspected at a desktop viewport. This is a browser preview of the returned App resource, not a verified Alexa device run.
- A local official MCP client exercised the standing-water wording without an explicit drain keyword, plus a later E:24 report after a drying check had been selected. Both produced a handover-ready scope result with no pending check; the structured and Markdown handovers retained the report and the SHA-256 digest recomputed independently. Local browser QA clicked the new scope proof, saw zero recorded checks and citations, an outside-scope card, and the same 64-digit fingerprint in the outer result and App. Clicking the cited streaks proof afterward still restored its deferred outcome and page-45/page-24 links. No local browser warnings or errors appeared. This verifies a bounded classification guard, not general diagnosis or safety coverage.
- Production Site version 15 repeated both UI paths: the scope proof retained the standing-water report with zero performed, deferred and cited checks; the App labelled it outside verified scope and matched the outer SHA-256 fingerprint. The cited path still selected the third-model rinse-aid check on pages 45 and 24, recorded a deferred outcome and matched its App fingerprint. No browser warnings or errors appeared. A fresh official MCP client exercised the production endpoint over new connections, including the ambiguous standing-water and later E:24 cases, and independently recomputed the handover digests. The production client evidence is in [HOSTED_MCP.json](HOSTED_MCP.json).
- The hosted D1 compare-and-swap receipt now depends on SQLite `changes() = 1` in the same batch. The [local D1 SQL probe](CAS_RECEIPT_PROBE.sql) returned zero receipts after a stale update and one after a valid update; it creates and removes only disposable probe tables. The local official MCP flow and browser's cited-check handover passed. Forty overlapping local requests against one revision yielded one accepted update and 39 rejected calls; retrying those rejected request IDs yielded zero successful ghost responses. That overlap run did not itself observe a pre-fix ghost; the SQL guard closes the potential interleaving in which the earlier unconditional receipt insert would have cached a rejected write.
- Production Site version 16 passed the fresh official MCP client flow and both zero-setup browser proofs. The cited result still restored one deferred check with pages 45 and 24, while the standing-water boundary selected no check and rendered the scope card; both App fingerprints matched their outer protocol results. The browser console had no warnings or errors. The D1 SQL probe ran locally only; it was not executed against the production database.
- A separate Apps-capable seven-path client run connected to the local Streamable HTTP endpoint at `/mcp`, negotiated protocol `2026-07-28` and the official MCP Apps extension, discovered all five tools, read the handover's self-contained `ui://` resource, exercised the then-current seven paths and three exact models with real local AI, recorded an outcome, and verified that the handover retained its observation across new client connections. Browser QA rendered the fingerprint and its no-authorship boundary in the actual MCP App. A request with an untrusted browser Origin was rejected with HTTP 403.
- Eighteen local-model scenarios pass: drying, food remnants, detergent residue, removable streaks, wash noise, cutlery rust, irreversible glass clouding, unpleasant interior odour, door-related starting, unclear symptom, plastic, inside-wall condensation, unsupported error/drainage issue, hazard, explicit no-hazard language, a technical phrase, already-recorded check and exhausted catalog. Full results and local latency/token observations: [LOCAL_AI_EVAL.json](LOCAL_AI_EVAL.json).
- A controlled same-fact retrieval evaluation now covers all nine source-backed paths. Across 54 blind-format reads by three local model families, structured handovers recovered 254/270 scored fields with zero critical errors; ordinary transcripts recovered 243/270 with five critical errors. The handover did not win every scenario, so the result supports an aggregate retrieval advantage rather than a universal one. These are synthetic machine-reader results, not evidence from people or completed repairs. Full inputs, outputs and scores: [HANDOVER_RETRIEVAL_EVAL.json](HANDOVER_RETRIEVAL_EVAL.json).
- Python compilation and JavaScript syntax checks pass.
- A source-only rehearsal copy under ignored `tmp/` passed its workflow tests and syntax checks. A focused scan found no email address, personal path or credential marker. Test-generated `__pycache__` remains excluded by the project `.gitignore`; the rehearsal intentionally contains no database or downloaded manual.
- The prior hosted build passed an actual Chrome desktop journey for drying, reload/resume, handover content and judge-mode boundaries. Later production releases receive separate browser verification recorded in the reliability review.

Two defects found and fixed during work: combined classification/selection occasionally assumed drying for an unclear report or failed to move past a recorded check. Classification now runs separately on user evidence; selection's schema is restricted to remaining catalog IDs. SQLite connections are explicitly closed after each transaction; the Windows test database cleanup exposed the original missing close.

The first main case retains the earlier clarification response as truthful QA history. Use a fresh demo case for final recording.

## Limits and next validation

- Small controlled evaluation set, not a robust safety or multilingual benchmark. No repair diagnosis, physical inspection or actual appliance outcome verified.
- Native mobile viewport, screen reader, keyboard-only end-to-end, long-running load and forced lost HTTP response after successful commit remain untested. Retry idempotency and stale versions have automated coverage; ordinary provider failure has UI coverage.
- Microphone capture was not started because accepting a Chrome microphone permission requires separate user authorization. Chrome exposed the API and the control remained enabled, but transcript accuracy and permission-denial UI remain unverified. The app states that Chrome may use an online recognition service and never auto-submits a transcript.
- No native Alexa account connection or voice/device execution tested. The product is labelled as an Alexa+ experience simulation.
- No owner/technician feedback or baseline comparison yet. Do not claim saved repair cost, repair accuracy, market demand or a better win probability.
- Independent owner/technician feedback remains missing. The public submission and current hosted video are live; Devpost's text and YouTube link still describe an earlier release and need an external edit before judging.
